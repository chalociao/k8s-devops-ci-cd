apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample-js-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: sample.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sample-js-service
            port:
              number: 80
